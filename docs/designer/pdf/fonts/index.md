# Fonts in PDF

PDF fonts must be configured for embedding.

Key topics:

- Font metrics
- Embedding modes
- FOP configuration
- Character encoding

Proper configuration ensures consistent rendering and accessibility.